import React from "react";
import styled from "styled-components";

const Logo = () => <Heading>watchers</Heading>;

const Heading = styled.h1`
  font-size: 2rem;
  font-weight: 600;
  text-transform: capitalize;
`;

export default Logo;
